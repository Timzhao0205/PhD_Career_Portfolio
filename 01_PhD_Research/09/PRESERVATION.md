# Preserved evidence and clean-rebuild boundary

This native package starts a new workflow state. It does not claim to import
the partial A10 pilot that exists only in the user's unuploaded V4.1 Windows
run directory.

Preserved:

- all 419 unique files from old Folder 06;
- all completed recent Folder 06 outputs, logs, audits, model records, pilots,
  state, and documentation, while mapping its byte-identical `src/06` tree to
  the one canonical old Folder 06 copy;
- the complete PhD/Option 2 archive;
- the complete startup corpus except its byte-identical duplicate old Folder
  06 tree and nested transport ZIP;
- prior chat/history;
- the 126-item score-free blind pool;
- the reviewed stage specifications and source/literature/model policies;
- an inert archive of prior V4.1 documentation and stage specifications.

All five supplied source attachments passed full ZIP CRC or byte-read checks
before packaging. `evidence/SOURCE_MANIFEST.json` records every included file,
source member, size, and SHA-256 plus every safety rename and duplicate map.

Historical nested `.claude` folders and executable-looking files were renamed
in the expanded copy. Their bytes remain preserved as evidence, but they cannot
become project configuration or runtime code.

Not preserved as active runtime:

- any V4/V4.1 PowerShell orchestration, hooks, validators, or checkpoint code;
- prior runtime acceptance state;
- the old session's in-memory context;
- any unverified claim that a requested model was actually observed.
