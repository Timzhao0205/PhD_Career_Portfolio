# Checkpoint — stage 20_observability (complete)

- **Stage/gate:** `20_observability`, attempt 1, cycle 0 — all acceptance
  gates satisfied; stage complete.
- **Model/effort:** Fable 5 / xhigh (requested and used; no auxiliary
  models, no downgrade event).

## Completed outputs

1. `outputs\02_OBSERVABILITY_AND_IDENTIFIABILITY.md` (654 lines):
   measurement model (direct + integrated-coil forms, §2 with all seven
   stage-required definitions); Theorem 1 two-parameter gauge-group
   non-identifiability proof (§3); identifiable invariants (§4); all nine
   required cases with per-case algebra + numerical rank cross-reference
   (§5); all six required confounding tests (§6); numerical protocol table
   (§7); 8-item assumptions register (§8); source support summary (§9).
2. `outputs\02_MUTUAL_CALIBRATION_FEASIBILITY.md` (190 lines):
   plain-language direction-dependent verdict; 13-row × 6-column
   feasible/conditional/not-feasible matrix; minimum reference/excitation
   list per objective; radiation-tracking implications; honest scope
   statement.
3. `outputs\02_ESTIMATOR_REQUIREMENTS.csv`: 15 rows (CASE1–CASE9 +
   CONF1–CONF6), exact 15-column stage header, parse-verified; all
   evidence_ids resolve against the 219-row final ledger.

## Analyses completed

- Structural proof: the unreferenced pair has an exact 2-dim gauge orbit
  (scale α: B↔S_H,K_C common-mode; DC β: b_H↔static field) for every
  waveform — the required non-identifiable counterexample.
- 16 deterministic complex-step FIM rank tests
  (`tools\observability_rank_tests.py`, output retained in
  `tools\observability_rank_tests_output.txt`): CASE A 5/7 (both analytic
  nulls matched ~1e−15; ratio S_H/K_C identifiable, S_H alone not), B 4/5,
  C1 6/8 vs C2 8/8 (zero-field anchors), D 4/4 vs D-flat 2/4, E 6/7 vs
  E-collide 5/7 (spectral-collision counterexample), F 2/5 (quasi-static),
  G 7/9 (S1/S2 identifiable), H-broad vs H-narrow (pole s.v. 1.2e−1 →
  1.4e−2, ~ω/a scaling), I 4/4 vs I-ramp 3/4 (g·r+m null — coil gain vs
  integrator drift needs dB/dt variation), J-corr 1/2 vs J-decorr 2/2
  (temperature-vs-dose collinearity).
- Validation pass: CSV header/rows parsed; 49 + 11 cited source IDs and
  all cited claim IDs verified present in `01_SOURCE_LEDGER.csv` /
  `01_EVIDENCE_MAP.csv`; no invented IDs.

## Key verdicts (for downstream stages)

- Hall→coil: feasible, hardware-proven direction (C02); coil→Hall: gain
  only, conditional (trusted coil chain + ΔB excitation), offset never;
  all-drifting: not identifiable (differential ρ-alarm only, no
  attribution); quasi-static: nothing identifiable (worst case); absolute
  scale always external (case 4 vacuum current+model is the workhorse).
- Literature NOT overextended: C06/C14 restated — no fusion/radiation
  coil-calibrates-Hall demonstration exists; GaN Hall neutron drift
  magnitudes Unknown, so excitation cadence cannot be sized yet.

## Unresolved questions (honest carry-forward)

- Metallic-Hall radiation-null reference rests on single source R071.
- H059 (coil-calibrates-Hall precedent) abstract-level only.
- Drift-rate magnitudes for GaN devices Unknown (C14) — bench/irradiation
  item for stages 30/60.

## Files safe to reuse

- All three `outputs\02_*` files; `tools\observability_rank_tests.py`
  (+ output txt, verification evidence); `tools\build_02_estimator_csv.py`
  (kept: deterministic CSV source, avoids hand-editing quoting errors if
  stage 30/60 refine estimator requirements).

## Next action

Run/resume the parent launcher into `30_radiation_compensation`.
