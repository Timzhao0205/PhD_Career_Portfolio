# Checkpoint — stage 10D (`10d_evidence_merge`), Fable 5 / xhigh, attempt 1

- **Stage/gate status:** COMPLETE — all numeric, schema, uniqueness, and
  new-source gates PASS (validator: 21/21 checks,
  `tools\validate_10d_outputs.py`, exit 0).

## Completed outputs and counts

| File | Content |
|---|---|
| `outputs\01_SOURCE_LEDGER.csv` | 219 rows, exact shared header; 215 `verified_peer_reviewed` (gate ≥120), 4 `peer_review_uncertain` (H006, R015, R016, R033); 0 duplicate source_ids/normalized DOIs/normalized titles |
| `outputs\01_NEW_SOURCE_AUDIT.csv` | 219 rows 1:1 with ledger; 202 new (198 verified — gate ≥75) / 17 reused vs folder 06 |
| `outputs\01_EVIDENCE_MAP.csv` | 37 typed claims (C01–C37); all cited IDs exist in ledger |
| `outputs\01_SOURCE_COVERAGE.md` | counts by status/tier/topic/year/access/role/new-reused; dedup method; 10 unresolved gaps |
| `outputs\01_HYBRID_LITERATURE_REVIEW.md` | prior-art timeline, demonstrated-vs-unproven, mandated drift-correction≠radiation-calibration statement (§4), novelty constraints |
| `outputs\01_RADIATION_LITERATURE_REVIEW.md` | mechanism/condition synthesis, cross-species warnings, calibration implications (§6 incl. mandated statement), 7 gaps |
| `outputs\01_APPLICATIONS_ALTERNATIVES_REVIEW.md` | requirements, incumbents, 7 alternative families, poor-fit vetoes; outreach priority explicitly deferred to stage 40 |

Topic quotas (verified): hybrid/coil 70 (≥25), radiation 79 (≥30),
applications/alternatives 76 (≥25), calibration/observability 50 (≥20).

## Verification performed (this stage, independently)

- All 219 final DOIs re-queried against the live Crossref API: 214 exact
  title/year matches; 5 old IEEE proceedings records lack a Crossref year
  field but match title+authors exactly (noted per-row). Results in
  `tools\crossref_verification_10d.json`.
- All 6 PMC IDs behind `full_text` claims confirmed via NCBI eutils.
- Semantic Scholar content re-checks on 5 flagged rows; H059's abstract
  retrieved and read — its coil-calibrates-Hall claim CONFIRMED.

## Corrections made (documented in the affected rows)

- Cross-lane merges: R005→H007, R077→H031 (same DOIs; tags/claims merged).
- P063 upgraded to `verified_peer_reviewed` (15-author list resolved).
- P008 first author corrected (Y. Chen → D. L. Chen, per Crossref).
- P011 author list resolved (Y. Zhang et al.).
- R011 tier A→B (digital Hall switch, relevance re-evaluation).
- H059 kept tier A with claim-confirmation note.

## Files safe to reuse

All seven `outputs\01_*` files; `tools\validate_10d_outputs.py` (re-runnable
gate validator); `tools\verify_crossref_10d.py` + `rows_for_verification.json`
+ `crossref_verification_10d.json` (verification evidence, re-runnable).
Lane files in `evidence\` unchanged.

## Unresolved questions (carried forward)

The 10 gaps in `01_SOURCE_COVERAGE.md` §4 — chiefly: no bare GaN/AlGaN
Hall-plate neutron data; no in-situ material-diverse Hall recalibration
demonstration; no joint gain+bias+state identifiability proof; 99/215
verified rows `metadata_only` (publisher access ceiling).

## Next action

Run/resume the parent launcher into stage `20_observability` (per
`EXECUTION_PLAN.md` ordering), which should consume
`outputs\01_EVIDENCE_MAP.csv` claims C03/C04/C06/C07/C11/C21.

- **Model/effort:** Fable 5 / xhigh (this stage's assigned model produced all
  accepted outputs and this final reconciliation; auxiliary models: none —
  research subagents were used only in stages 10A–10C, not in 10D).
