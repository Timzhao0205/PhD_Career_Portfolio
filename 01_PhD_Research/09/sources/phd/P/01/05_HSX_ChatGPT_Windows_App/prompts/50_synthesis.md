Produce the final self-consistent recommendation. Read all inputs, prior results, current outputs,
drawings, ledgers, and red-team findings. Correct every resolvable BLOCKER/MAJOR issue in new final
artifacts; leave unresolved items as explicit evidence/measurement gates. Re-run mapping and
dimension checks after corrections. Never hide uncertainty behind a confidence score.

Write:

- `outputs/FINAL_RECOMMENDATION_250C_UHV.md`: direct answers to all three user questions, final
  LCC bond/external connection, three-board architecture, reusable ceramic design, alternatives,
  build order, budget strategy, qualification plan, and decision gates;
- `outputs/FINAL_ACCEPTANCE_CHECKLIST.md`;
- `outputs/FINAL_PATCH_AND_PROVENANCE.md`, identifying reused historical data and new corrections;
- `outputs/FINAL_FILE_INDEX.md`, linking every report, CSV, SVG, and CAD artifact.

The package section must state: tapped ceramic thread count (target zero), unique ceramic geometry
count, post-fire operations, how each bonded LCC is independently replaced, how its connection
remates, sacrificial/replaceable parts, tool access, and 1/3/10-set cost basis. Ensure every final
artifact agrees on p1-p4, LCC pads/chamfer, feedthrough pins, J1 pins, axes, and units. Label any
unverified fabrication detail `HOLD - DO NOT FABRICATE/ORDER`.

Update `outputs/OPEN_GATES.md`. The final report must declare `COMPLETE_WITH_OPEN_GATES` unless no
fabrication- or purchase-blocking item remains.
