---
description: Quick lookup from the readout spec sheet (fast + cheap)
argument-hint: e.g. "J3 pinout", "emulator step size", "demod sign rule"
model: haiku
---
Answer the question below using ONLY
`02_HSX_Hall_Sensor_Readout/docs/SPECS.md`. Read that file, quote the
relevant value(s) with units, and stop. No web search, no re-derivation,
no extra commentary. If the answer is not in SPECS.md, say so in one
line and name which project doc likely has it instead.

Question: $ARGUMENTS
