# Pre-restart audit archive

This tree is the complete mission-runtime record from before the
Fable-final-result policy and full restart on 2026-07-24.

It is audit-only:

- do not use its completion marker to skip active stage 00;
- do not resume its Claude session IDs;
- do not treat its outputs or evidence as accepted current-stage work;
- do not append new active-run records here.

Archived contents:

- `state`: 54 files;
- `logs`: 42 files;
- `outputs`: 5 files;
- `evidence`: 1 file;
- total: 102 files.

The active workflow is in the sibling `state`, `logs`, `outputs`, and
`evidence` directories.
