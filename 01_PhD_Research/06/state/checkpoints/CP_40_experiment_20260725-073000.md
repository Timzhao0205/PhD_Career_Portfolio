# Checkpoint — stage 40_experiment — 2026-07-25 07:30 (-07:00)

## Status

`40_experiment` **COMPLETE** (attempt 1, cycle 0, session
f2b49370-0ee2-4fda-b3aa-dcad1e3fe27c, run_2026-07-24_231243_734).
Requested model/effort: **Fable 5 / Extra High** (launcher-recorded in
`state/CHATGPT_HANDOFF_STATE.json`: requested `fable`/`xhigh`, reported
`claude-fable-5`, no downgrade, no security fallback). All stage work —
evidence reading, all four outputs, the validator, and this checkpoint —
was performed in the Fable main session; **no auxiliary models or
subagents were invoked**.

## Files produced this stage

- `outputs/04_MEASUREMENT_REQUIREMENTS.csv` — 34 rows, exact 14-column
  required header. ID groups: A-01..A-06 (supplied-data analyses),
  B-01..B-06 (chain verification/bandwidth/noise/parasitics),
  C-01..C-08 (calibration + characterization), D-01..D-03
  (repeatability), E-01..E-03 (reference strategy), F-01..F-07
  (campaign), G-01 (retroactive 2025 tesla conversion). Every row:
  driver, min/preferred design, hardware gates, justified replicates,
  acceptance metric, uncertainty component, dependency, priority,
  fallback.
- `outputs/04_HSX_EXPERIMENT_PLAN.md` — 13 sections: design rules
  (bench-before-machine, nothing-assumed, anomaly-first); inventory
  gates I-1..I-9; UW gates U-1..U-9 (single advisor-authorized July
  email); go/no-go ladder G0/G1/G-cal/G-die/G2 (aligned to stage 20
  G1-G5); bench phase 0; DC + frequency calibration incl. the
  C-03 voltage-bias-mode S_v calibration needed because the 2025 data
  used the 2023 G=200 chain; sign/orientation protocol; 8-quantity
  characterization table; WP-B >=3-die design with honest single-device
  fallback; bench-vs-HSX reference separation (no gaussmeter assumed);
  coil-only anchor + pose; shot matrix with interleaving honesty +
  metadata schema + failure handling; Tier-1 SENSL vs Tier-2 RSI
  packages; burden ~19-28 bench/desk-days to Tier 1; zero-cleanroom
  route; open-gate handoff.
- `outputs/04_DATA_ANALYSIS_PLAN.md` — immutability/provenance (SHA-256
  chain), metadata schemas, preprocessing pipelines (2025 archive +
  spun readout), transfer-function equations (current-bias; voltage-bias
  changes-only conversion with V_off honesty), anchor comparison
  criterion, 1:1 metrics (residuals/Bland-Altman/r(lag) with block
  bootstrap/effect sizes), repeated-measures rules (shot = primary
  unit), leakage safeguards (held-out field points, frozen parameters,
  shot-level splits for any WP-D work), figure-to-claim map (16
  artifacts), reproducibility + release structure.
- `outputs/04_UNCERTAINTY_AND_STATISTICS_PLAN.md` — notation + models;
  statistical-unit hierarchy (wafer/die/module/channel/session/shot/
  time-sample) with unit-and-n reporting rule; worked symbolic budget
  (u(m)/m ~ 2.1% with placeholder set, coil constant 92% of variance;
  trace-level u(B); G-01 budget with named unmeasured terms; anchor
  joint budget); replication logic from u(s)/s ~ 1/sqrt(2(n-1)) and
  detectable-effect formulas — no invented sample sizes, conditional
  illustrations labeled; sensitivity analysis (one-at-a-time doubling,
  threshold questions, GUM-vs-MC per S0220); pre-drafted limitations
  language for six shortfall scenarios.
- `state/tools/40_validate.py` — deterministic validator.

## Validation

`python state/tools/40_validate.py` → **PASS**: 34 rows / 34 unique IDs
matching `[A-G]-\d\d`; exact header; priorities well-formed; no fences;
UTF-8; 32 relative links resolve; 25 `[S####](doi)` citations resolve to
the 231-row ledger with DOI match; 32 bare S-ID mentions resolve; 252
requirement references in the MD files all exist in the CSV.

## Open gates (carried forward)

1. Advisor decisions 3 (die supply for D-01) and 4 (UW email
   authorization covering U-1..U-9) — stage 20 §10.
2. Inventory gates I-1..I-9, most consequential I-4: post-campaign
   location/health of the deployed 2025 module (blocks C-03/G-01, i.e.
   the direct AE-01/AE-05 answer).
3. U-1: existence of co-located 2025 B-dot records decides whether
   R1-02 closes by analysis or waits for campaign #1.
4. C017 (~109x anomaly) open until B-01; C018 (Aug window) a target,
   not a booking; Tier-1 package engineered to survive both.
5. Stage 50 owns the pre-disclosure IP screen (hard gate before any
   arXiv action).

## Exact next operation

Launcher proceeds to stage `50_patent` (Fable 5 / Extra High per
`EXECUTION_PLAN.md`): cautious IP/prior-art analysis — screen only
concepts already supported by the supplied work (research screen, not a
legal opinion), honoring the advisor's IP-screen-before-arXiv
requirement and stage 20 §9's thin-claims expectation.
