# IMPACT_MAP — B20_align (PILOT SAMPLE — NOT FINAL)

Stage: `B20_align` | Mode: `PILOT` | Attempt: `1`
Named worker: `pap06-fable-xhigh` | Requested model/effort: Fable 5 / xhigh

Causal chains for the six pilot ideas, plus explicit with-Opt2 vs
without-Opt2 counterfactuals for THREE ideas (P3R2-D-02, P3R2-D-01 in full;
P3R2-D-10 as a deliberate zero-delta control). Throughout, "causal" means a
stated mechanism whose removal changes the outcome; "correlative" means
shared vocabulary, milieu, or theme that changes nothing when removed.
Opt2 = the B10-defined continuation (Element 1 WP-C traceable calibration;
Element 2 Hall+coil hybrid/identifiability; Element 3 reusable estimator
package T0), all currently proposed, folder-08 parts pre-redteam (C40).

## 1. Causal chains (all six)

### P3R2-D-02 (STRONG)
- **PhD→startup chain:** demonstrated Hall readout-chain design (C03/C13)
  → competence to build the scanner's Hall-array head and DAQ → higher
  probability the $120k blind-correlation experiment is executed
  competently. Proposed WP-C (C06, gated by C04) → traceable calibration +
  GUM/Allan discipline → the "transport-calibrated, signed conductor
  passport" differentiator (EV01/EV35 show the method exists at low field
  and the tesla-scale/harsh-regime combination is under-published).
  Packaging/deployment record (C46/C01) → credibility with magnet-builder
  buyers (merchant-magnet buyer class verified current: S-B20-02).
- **startup→PhD chain:** executing the scanner forces a real traceable
  calibration campaign, multi-channel readout, and industrial datasets →
  exercises Element-1 skills and could yield an instrumentation paper →
  but feeds neither the stellarator thesis (C26 gap d) nor the hybrid
  reverse direction (EV32); time is drawn from the P1 revision.
- **Correlative only:** "both are fusion-industry" — the product needs no
  stellarator physics, no GaN, no radiation data; removing the fusion
  framing (tape for cables/NMR) leaves the mechanism intact.

### P3R2-D-01 (MEDIUM)
- **PhD→startup chain:** bench weak-signal/EMI measurement discipline (C03)
  → relevant to false-trigger qualification, the venture's stated central
  engineering risk; proposed estimator-honesty/fusion methodology
  (C08/C10/C23/C31) → applicable to deterministic multi-modal detection
  statistics (the problem class B15 documents: EV06/EV23, P0033/P0038;
  monitoring gap named at review level, EV25/P0046). The chain's weakness:
  the product's sensing modalities (Rayleigh fiber, RF, voltage) and dump
  power electronics are not PhD-evidenced, and no Hall-based HTS-monitoring
  method exists anywhere in the corpus (EV26 — untested, not refuted).
- **startup→PhD chain:** driven-quench campaigns → a live application and
  collaborator base for FT-02-class estimator work → possible off-thesis
  publications; nothing feeds C04/C45/P1, and scope drift is high.
- **Correlative only:** "HTS magnets are fusion technology, the PhD is
  fusion diagnostics" — the mechanism test shows the venture's core
  excludes the PhD's core hardware; the felt affinity is industry milieu.

### P3R2-A-14 (MEDIUM, boundary)
- **PhD→startup chain:** demonstrated harsh-environment packaging practice
  (C46: LCC, wedge bonds, 150 °C vacuum bake, GDC shielding — done once)
  + drift-characterization mindset (C06 proposed; EV13 shows temperature
  drift is the recognized central axis) → credibility and method for
  qualification-currency selling. Not in the chain: 300 °C SiC/SOI
  mixed-signal design and 300 °C/1000 h packaging qualification — the
  venture's actual differentiator (its own record hires a packaging
  engineer first). EV31's loose-analogy adjudication applies to the
  materials-vocabulary part.
- **startup→PhD chain:** a 300 °C qualification lab could incidentally
  generate the AlGaN/GaN high-temperature Hall data the literature lacks
  (P0017's 2011 "insufficient published data," 15 years stale per B15) —
  concrete but unplanned; classified speculative.

### P3R2-C-13 (WEAK — the thematic trap, made explicit)
- **Tested and rejected chain:** "PhD is GaN → startup is GaN → synergy."
  The PhD's GaN is a Hall-sensing 2DEG; the startup's GaN is a power
  switch. Different physics, different failure modes, different design
  disciplines (EV31; EV30). Removing the word "GaN" from both descriptions
  removes nothing mechanical — the definition of correlation without
  causation.
- **Surviving sliver:** calibrated current-sense/diode-health telemetry
  under switching EMI (EV27/P0050 problem class) is a genuine but non-core
  overlap; the venture's own record locates its unproven core elsewhere
  (the OEM merchant socket).

### P3R2-D-10 (ADVERSE)
- **PhD→startup:** no chain — no shared hardware, method, dataset, or
  buyer; "control loops/estimators" is thematic. The direction is negative
  via timing: A30-verified primaries date the design-in window 2026-2028
  (awards executed 2026-07-09; JBCS from Q4-2026; awardee internalizing
  the phase-control layer), i.e., across the remaining PhD years.
- **startup→PhD:** negative chain — controlled/classified prime-facing work
  → publication restrictions → direct collision with G5 (accepted paper
  required; C49: zero) and the OTL disclosure gate (C33/C34).

### P3R2-C-07 (WEAK, killed)
- **No chain either way.** kA-class AFE rectifier retrofits and M&V
  performance contracts share nothing mechanical with Hall metrology
  (EV30/EV31). The venture is additionally impaired on A30-verified facts
  (45V construction-start before 2028-01-01; Ingeteam shipping since
  2023) — pursuing it buys neither startup position nor PhD progress.
  Retired.

## 2. Counterfactual A — P3R2-D-02 with vs without Opt2

**With Opt2 executed to plan** (C04 anomaly closed → WP-C run to its
u(k)/k ≤ 2% target with GUM/Monte-Carlo budget and Allan drift
characterization; T0 estimator built with honesty gates FT-02):
- The founder enters the D-02 decisive experiment holding a *demonstrated*
  traceable Hall-calibration capability — the exact scarce asset (EV35:
  the traceable+harsh+tesla combination is unpublished in the 62-paper
  corpus) that the product's "certified acceptance instrumentation"
  positioning requires, and the exact method (EV01/P0008 template) the
  blind Ic-correlation gate needs. Causal effect: raises the probability
  the $120k kill gate passes with a defensible uncertainty budget, and
  converts the "certification authority" moat claim from marketing to
  evidenced capability. Element 3's frozen-interface/regression discipline
  (C10/C31) causally supports the signed per-meter passport product.
- **Which part of Opt2 does the work:** Elements 1 and 3 only. Element 2
  (Hall+coil hybrid, identifiability theory, radiation compensation)
  contributes approximately nothing to D-02 — a tape scanner has no coil
  channel and no radiation environment. The synergy is with *half* of
  Opt2, not with Opt2 as a package.

**Without Opt2** (PhD stops at current state, or pivots to the C47
reconstruction fallback):
- Still transfers: demonstrated bench readout (C03/C13), packaging and
  deployment credibility (C46/C01). Lost: the traceability credential —
  calibration remains proposed-on-paper, the anomaly stays open, and the
  founder would build calibration credibility from scratch inside the
  venture (feasible — EV12 shows procedure/algorithm literature is open —
  but slower and without the differentiating track record).
- Net: alignment degrades from **direct leverage to adjacent leverage**;
  the venture stays viable (its kill gate is independent of the PhD), but
  the founder-specific edge shrinks materially.

**Causal vs correlative, stated:** the causal carriers are the calibration
methodology and Hall readout competence. The fusion-market framing, the
GaN die, and the stellarator deployment story are correlative color — they
change buyer conversations, not the instrument.

## 3. Counterfactual B — P3R2-D-01 with vs without Opt2

**With Opt2 executed to plan:**
- The transferable increment is Element 2/3 *methodology*: an
  honesty-gated multi-sensor estimator discipline (C23/C31/C48) applied to
  fused fiber/RF/voltage detection — directly relevant to the venture's
  central risk (false-trigger statistics under EMI; EV06/EV23). No Opt2
  hardware asset transfers: the hybrid probe is Hall+coil, the venture's
  stack is fiber+RF+voltage; the GaN radiation question (C29/EV09) is
  irrelevant to 20 K magnet monitoring as specified.
- Causal effect: modest — it upgrades one engineering sub-problem
  (detection-engine qualification) from generic competence to a
  differentiated method. It does not create buyer access, magnet access,
  or cryogenic practice (the record's own named founder gap).

**Without Opt2:**
- Transfer reduces to generic bench EMI-instrumentation discipline (C03).
  The alignment class stays MEDIUM either way.

**Finding:** the Opt2 delta on D-01 is small — which quantifies the
correlation trap: D-01 *feels* like the most PhD-aligned idea ("fusion
magnets!") but is nearly Opt2-insensitive, while D-02 (tape QC — less
fusion-flavored) is strongly Opt2-sensitive. Thematic proximity and causal
sensitivity anti-correlate in this pair.

## 4. Counterfactual C (control) — P3R2-D-10 with vs without Opt2

With Opt2, without Opt2, or with any PhD outcome whatsoever: nothing in the
D-10 chain changes. No Opt2 element touches electro-optic phase control,
and the interference mechanisms (classification vs publication; 2026-2028
window) are independent of PhD content. Zero counterfactual delta is the
signature of true non-alignment — and it demonstrates that Opt2 is a
*narrow* startup asset (it moves D-02 a class, D-01 a sub-problem, and
D-10 not at all), not a general-purpose venture credential.

## 5. What this map does NOT claim

- No claim that any venture succeeds or fails commercially — venture
  viability gates (kill rules) are quoted from the corpora's own records
  and are separate from alignment.
- No claim that Opt2 will be executed: WP-C is gated by the open C04
  anomaly; folder-08 content is pre-redteam (C40); the deployed module's
  location is unknown (C45). Every "with Opt2" branch above is explicitly
  conditional on those gates clearing.
- No ranking of ideas — that is B40's job. This map only states, per idea,
  which causal channels exist, in which direction, and what would change
  them.
