Raw capture CSVs live here. Files >1 MB are gitignored -- the run log
(RUN_*.md) is the durable record: it must name every data file with
enough conditions to re-run the analysis. Naming:
cal_<ABV>pct_<date>.csv, run_<date>_<n>.csv.
